spring:
  application:
    name: config
  profiles:
    active: dev
  cloud:
    config:
      server:
        git:
          uri: https://github.com/marcusMintchip/SpringCloudConfig #远程仓库配置
          #uri: file://${user.home}/config-repo　＃本地仓库配置
          searchPaths: '{application}/{profile}'
      label: master

server:
  port: 8888
